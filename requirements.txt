# Core pipeline (runs everything except the deep models)
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
h5py>=3.8          # read DASPS preprocessed .mat (MATLAB v7.3 / HDF5)
openpyxl>=3.1      # read participant_rating_public.xlsx labels

# Deep models (week 3+): BandTensorCNN, EEGNet
# torch>=2.0

# Explainability backends for deep models (SHAP / Grad-CAM)
# shap>=0.44

# Optional richer preprocessing on the raw .edf
# mne>=1.5
